// Placeholder until a live Supabase project exists — regenerate with
// `supabase gen types typescript` (or the Supabase MCP `generate_typescript_types` tool)
// once supabase/migrations/*.sql has been applied to a project.
export type Database = any
